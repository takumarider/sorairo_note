<h1>時間枠一覧</h1>

<div class="admin-actions">
  <%= link_to "＋ 新規時間枠を追加", new_admin_slot_path, class: "btn btn-primary" %>
  <%= link_to "📅 カレンダー表示", calendar_admin_slots_path, class: "btn btn-secondary" %>
</div>

<table>
  <tr>
    <th>開始</th>
    <th>終了</th>
    <th>公開</th>
    <th>操作</th>
  </tr>

  <% @slots.each do |slot| %>
    <tr>
      <td><%= slot.start_time.strftime("%Y-%m-%d %H:%M") %></td>
      <td><%= slot.end_time.strftime("%Y-%m-%d %H:%M") %></td>
      <td><%= slot.available ? "○" : "×" %></td>
      <td>
        <%= link_to "編集", edit_admin_slot_path(slot) %> |
        <%= link_to "削除", admin_slot_path(slot), data: { turbo_method: :delete } %>
      </td>
    </tr>
  <% end %>
</table>

<style>
.admin-actions {
  margin: 20px 0;
  display: flex;
  gap: 10px;
}

.btn {
  padding: 8px 16px;
  text-decoration: none;
  border-radius: 4px;
  font-size: 14px;
}

.btn-primary {
  background: #007bff;
  color: white;
}

.btn-primary:hover {
  background: #0056b3;
}

.btn-secondary {
  background: #6c757d;
  color: white;
}

.btn-secondary:hover {
  background: #545b62;
}
</style>

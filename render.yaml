services:
  - type: web
    name: sorairo-note
    env: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - fromDotEnv: true
    autoDeploy: true
